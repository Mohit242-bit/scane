import { NextResponse } from "next/server"
import type { NextRequest } from "next/server"
import { createServerClient, type CookieOptions } from '@supabase/ssr'

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl
  
  console.log(`🔍 Middleware: ${pathname} - ${new Date().toISOString()}`)
  
  // TEMPORARILY DISABLED FOR OAUTH DEBUGGING
  // The middleware was causing OAuth redirect loops
  // We'll add back protection later once OAuth works
  
  return NextResponse.next()

  // ALL ROUTE PROTECTION TEMPORARILY DISABLED
  // This was causing OAuth redirect loops
  // Will be re-enabled after OAuth is working
}

export const config = {
  matcher: [
    "/admin/:path*", 
    "/partner/:path*", 
    "/api/admin/:path*",
    "/((?!_next/static|_next/image|favicon.ico|api/auth).*)"],
}
